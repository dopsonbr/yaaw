# Rewrite Deferred Issues Log

Running list of **minor** issues discovered during the rewrite that are
intentionally deferred to be addressed at completion (or as fast-follows). Only
minor items land here — every **major** issue must be addressed inline during
the chunk that surfaces it (per the owner's instruction).

Each entry: severity (minor/trivial), where it was found, what it is, and the
suggested fix.

| # | Severity | Area | Issue | Suggested fix |
|---|----------|------|-------|---------------|
| 1 | minor | `Theme/DraculaTheme.swift` | swiftlint warnings (not errors): `type_body_length` 385>250 and one `large_tuple` (3-member). File is ported verbatim for pixel-exact appearance parity. | Split `ThemeCatalog.themes` array into an `extension` in a sibling file; refactor the 3-tuple into a small struct. Both under the error threshold, so non-blocking. |
| 2 | minor | `Persistence/YAAWConfiguration.swift` (579), `KeyboardShortcutSettings.swift` (447) | `file_length` **warnings** (400<n<800). Cohesive type groups; under the 800 error ceiling. | Optional finer split (e.g. peel `KeyboardShortcutAction` out of the shortcuts file). Non-blocking. |
| 3 | minor | `Fonts/BundledFontCatalog.swift` (`performRegistration` 58 lines), `YAMLConfigurationStore.swift` (`render` 108 lines) | `function_body_length` **warnings** (50<n<120). `render` is one big verbatim template literal. | Extract the font-directory enumeration; leave `render` (a single template string) as-is. Non-blocking (under 120 error). |
| 4 | minor (perf) | `Persistence/SQLiteYAAWStore+Save.swift` / `+Load.swift` | Release perf (`YAAWKitPerf`): save full-snapshot @10k = **109 ms** (target ≤30 ms; 3.5× better than the 381 ms baseline) and load @10k = **17.5 ms** (target ≤10 ms). Single-edit @10k = 0.042 ms (≤2 ms — the hot path, met). The residual cost is Swift binding of ~12 TEXT columns × 10k rows. | Switch UUID primary/foreign keys + date columns from TEXT to 16-byte BLOB / REAL to cut per-row String allocation and `SQLITE_TRANSIENT` copies (schema migration v18; broad ripple across bind/read helpers). Only matters at the synthetic 10k-thread extreme; realistic workspaces save in <5 ms. |
| 5 | minor (env) | toolchain | `swift test -c release` cannot build any XCTest target importing `YAAWKit` on Swift 6.3.2 (was the `isolated deinit` optimizer cycle, now fixed for the *library*, but XCTest-in-release with this module still warrants caution). Release perf gate runs via `YAAWKitPerf` executable instead (D-010). | Re-test `swift test -c release` on a future toolchain; if fixed, the XCTest benchmarks can also serve as the release gate. Debug XCTest benchmarks run fine today. |
| 6 | minor (perf) | `FileBrowser/BackgroundFileIndexer.swift` | Release perf (`YAAWKitPerf`): cold-index 50k = **2828 ms** (target ≤1500 ms). Faithful port of the original walk — no algorithmic regression; fuzzy 50k (41.6 ms ≤400 ✓) and tree-builder 50k (56.9 ms ≤61 ✓) both pass. The same machine misses the Chunk A persistence gates proportionally, so this is hardware-relative calibration, not a Chunk B regression. | Re-measure on the calibration machine; if a true regression, profile the FileManager enumeration + per-entry ignore matching. The walk itself is unchanged from `main`. |
| 8 | minor (integration) | `AgentCLI/SessionBindingActor` | Live session-capture signal currently uses the polled capture-log mechanism (ported). The plan wants it pushed as an XPC `RenderEvent` from the helper (Chunk D), and drift (`CatalogMetadataResult.driftDetected`) surfaced as visible thread state via `ActivityStore` (Chunk E). The actor already returns the drift signal + records a diagnostic; the UI/event wiring lands in D/E. | Wire `RenderEvent.activity`/`sessionId` (Chunk D) into `SessionBindingActor` linking, and `driftDetected` → `ActivityStore` thread state (Chunk E). |
| 10 | minor (test) | `Tests/YAAWKitTests/` (Chunk E) | ~5 AppModelTests cases asserting the in-process terminal-session manager lifecycle (`testProjectTerminalSessionIsScopedToSelectedActiveThread`, `…RelaunchResetsCaptureOffset`, `…ClosingNvimTabTerminatesOnly…`, `testReplacingActiveTerminalSessionRecordsTerminatedState`, `testTerminalRuntimeStateIsNotPersisted`) couldn't port — they assert the old in-process `TerminalSessionManaging` lifecycle the rewrite replaces with the XPC `RenderHostClient` (D-part-2). Surface interactions are driven through the `RenderSurfaceManaging` fake; launch-descriptor construction parity IS covered. | Re-express these against `RenderHostClient` lifecycle + the crash-isolation E2E probe in Chunk G / D-part-2. |
| 12 | **RESOLVED** (2026-06-12) | `src/RenderHost/` + `src/App/` compositing | Compositing now **works**: terminals render in-pane via shared IOSurface (Candidate 2). CAContext/CALayerHost (Candidate 1) was found non-viable cross-process and abandoned (D-016/D-018, ADR-004). Verified on a real run: agent + bottom terminals composite, helper invisible, no focus steal. | Done. Remaining polish split into #20–#22 below. |
| 20 | **RESOLVED** (2026-06-12) | `App/TerminalSurfaceHostView.swift` + `RenderHost/TerminalHostController.swift` | Verified the composited terminal text is already crisp 1:1 (helper renders the IOSurface at the pane's backing-pixel size, so `.resize` maps 1:1 — confirmed at 3× native zoom). The PTY winsize is authoritatively driven by libghostty's `receive_resize` callback, not the app's 8×17 estimate. Cleaned up the helper to prefer the real fitted grid (`TerminalViewState.surfaceSize`) when starting/resizing the PTY (estimate kept only as a pre-attach fallback) and documented the `resizeOrStart`-then-`fitToSize` ordering (D-019). | Done — no metric round-trip needed. |
| 21 | **partially addressed** (2026-06-12) | `RenderHost/TerminalHostController.swift` + `App/RenderHostClient.swift` | The big idle-CPU cost was not the pump but per-frame `@Published` churn invalidating SwiftUI; frames now bypass `@Published` (delivered to the pane layer via a registered sink), so a live terminal idles at ~0.3% main-app CPU (D-020). The pump is now an **adaptive** deduped seed poll (~30 fps active → ~5 fps idle). | Remaining: a fully event-driven push needs libghostty's `onPostRender`, which libghostty-spm 1.2.4 does not vend publicly — would require a `Patches/` patch against the pin. Low value now that idle CPU is ~0. |
| 23 | minor (perf) | `AgentCLI/SessionBindingActor` + `Stores/WorkspaceStore` | order-up (large monorepo, many worktree threads) still produces a **one-time** background CPU burst at launch while `reconcileLoadedUnboundSessionLinks` parses each unbound thread's session catalog (now off the UI critical path, settles to idle). Catalogs are cached per `(family, workingDir)` + exact-link results memoized by signature, so the steady state is ~0.3%. | If the launch burst matters, make reconcile lazy (only when a thread is first selected — the 1 Hz poll already links the selected thread) or persist a catalog parse cache keyed by file signatures. |
| 24 | minor (perf/UX) | `FileBrowser/BackgroundFileIndexer.swift` | The eager index walk is bounded (entry cap 200k + 30 s backstop, D-023) so it can't hang, but it is depth-first: a tree past the cap truncates lopsidedly (whole later top-level dirs missing), and an order-up-class tree (~157k entries) shows "Indexing…" for ~15 s on first open before the (cached) full index appears. | Breadth-first or depth-limited eager indexing for a more even partial tree; and/or a faster enumeration path (raw `readdir`/`fts` instead of `FileManager.enumerator` + per-entry `resourceValues`) to cut the ~15 s first-index time. |
| 25 | **needs investigation** (GUI, 2026-06-12) | `App/FileBrowserPanelView.swift` (SwiftUI) | **Intermittent empty Files tree.** On some launches the Files panel shows the status line ("N items, M collapsed directories") and the indexing spinner but **renders no tree rows**, even though the SQLite cache holds the entries (verified: /yaaw row has file_count=316 == stored_entries, cache key matches git-identity/fingerprint/schema → `cachedIndex` hits) and the file index itself works (order-up indexed 157k, FileBrowserTests green). So the entries are in `state.entries` but `treeRows`/the `LazyVStack` don't render them. It rendered correctly in other captures this session (yaaw-cal.png), so it is intermittent — a SwiftUI view-lifecycle/state-observation issue in `FileBrowserPanel` (the `onAppear`/`onChange(of: state.entries)` rebuild of `treeRows`, or a ScrollView/LazyVStack layout). Not introduced by the file-index changes (it reproduces in the clean E2E fixture too) — most likely a pre-existing Chunk-F view bug, unverified until the first full GUI run. The headless E2E suite does not assert tree *rows* (only the count + screenshot), so it passes. | Reproduce deterministically (likely needs a debug build with logging in `rebuildTreeIndexAndRows`/`rebuildVisibleTreeRows`); consider deriving `treeRows` in `body` (or via `.task(id:)`) instead of `@State` + `onChange`, and verifying the ScrollView gets non-zero height at all window sizes. Add an E2E assertion that the files tree renders ≥1 row. |
| 26 | runtime-verify (GUI) | `RenderHost/BrowserHostController.swift` | Browser-pane compositing (#22) is implemented (WKWebView `takeSnapshot` → shared IOSurface, vertically flipped, snapshot pump, window ordered-in below desktop) and builds + the IOSurface *wire* is verified working (terminal/nvim/lazygit surfaces composite via the same `frameReady(surface:)` path). The browser-pane *visual* render was **not** confirmed in this session: the E2E suite has no browser visual state, and manually driving the browser preview needs selecting a markdown/HTML file in the Files tree — blocked by #25 (empty tree) plus synthetic-click/focus limitations in this multi-display + active-meeting environment. | Verify by opening a markdown/HTML file in the browser preview on a focus-clean single-display machine (or add a seeded browser-preview visual state to the E2E suite), confirming the snapshot renders right-side up and updates. |
| 27 | minor (perf/UX) | `Stores/WorkspaceStore` + `AgentCLI/SessionBindingActor` | The deferred load-time session-link reconcile (D-022) keeps the UI from blocking, but on a workspace with many unbound worktree threads (order-up) its one-time background burst saturates the cooperative pool for ~15 s, which can delay other actor-backed work (e.g. the file index publish) on launch. | Reconcile lazily — only the selected thread at launch (the 1 Hz poll already links the selected thread), reconciling others on first selection — and/or run the reconcile Task at `.background` priority so the file index / UI actors preempt it. |
| 22 | follow-up | `RenderHost/BrowserHostController.swift` | Browser-pane compositing: WKWebView hosts its content in WebKit's own process via a remote layer, so its backing layer doesn't expose a plain IOSurface like Ghostty's IOSurfaceLayer does. The browser pane won't composite via the current IOSurface path. | Use `WKWebView.takeSnapshot` into an IOSurface on a cadence, or a WebKit remote-layer hosting path. Terminals (the primary surface) work. |
| 13 | minor | `src/RenderHost/{RenderServiceDelegate,TerminalHostController}.swift` | Two `@unchecked Sendable` in the helper: `RenderEventReply` (wraps the `@objc` XPC reply proxy) and `TerminalDriverCallbacks` (bridges the PTY read thread's `@Sendable` closures). Both are documented XPC/threading bridge objects — the idiomatic pattern for NSXPCConnection proxies. Not in YAAWKit. | Acceptable; revisit if a Sendable-by-construction wrapper becomes feasible. |
| 15 | **RESOLVED** (2026-06-12) | `RenderHostClient` ↔ `build_and_run.sh` | The helper is packaged as an XPC service bundle (`Contents/XPCServices/dev.dopsonbr.YAAW.RenderHost.xpc`); the per-surface `NSXPCConnection` resolves and surfaces composite via shared IOSurface. Verified on real runs. | Done. |
| 16 | **RESOLVED** (2026-06-12) | `src/App/` appearance | Appearance parity verified against `docs/examples/screenshots/current/*` on real runs — native unified-compact titlebar, liquid-glass sidebar materials, theme/fonts, layout all match. | Done (spot-checked; an automated pixel-diff against baselines could be added to the suite). |
| 18 | **RESOLVED** (2026-06-12) | `src/E2E/` + `scripts/test-e2e.sh` | The full headless E2E suite passes (exit 0) on a machine with screen + accessibility: durable-state runner, release perf gates, terminal visibility (region/pixel checks), workspace shortcuts, settings editor, crash-isolation (`kill -9` → app+siblings survive, pane recovers), 15 visual states, no-focus-steal between every probe. The suite was reconciled from the overlay model to the IOSurface model (D-024); the typed-text keyboard probe is `--headed`-only (D-024/D-025). | Done. |
| 19 | minor (test) | `Tests/YAAWKitTests/FileBrowserTests.swift` | ~11 spec-listed tests drive `AppModel` (lazy-expand indicator, per-thread selection/expansion memory) which doesn't exist until Chunk E. The underlying FileIndexActor behaviors (dedup, cached-while-refresh, lazy subtree merge+search, concurrent-expand coalescing) are tested directly in `FileIndexActorTests`. | Re-home the AppModel-level assertions when AppModel's replacement (the stores) lands in Chunk E. |
