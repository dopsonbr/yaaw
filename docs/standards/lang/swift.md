# Swift Standard

- Use Swift for the primary app implementation.
- Keep public APIs small and behavior-oriented.
- Format Swift source and `Package.swift` with `scripts/format.sh`.
- Lint Swift source and `Package.swift` with `scripts/lint.sh`.
- Avoid testing private functions directly.
- Keep platform-specific behavior explicit and documented.
