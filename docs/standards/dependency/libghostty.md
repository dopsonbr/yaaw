# libghostty Standard

- Use `libghostty` for every embedded terminal surface.
- Apply consistent lifecycle handling across project, global, `nvim`, and `lazygit` terminals.
- Preserve runtime terminal state while the app is running; do not require restart persistence.
