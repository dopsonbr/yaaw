# nvim Standard

- Run `nvim` inside the right panel through an embedded terminal.
- Resolve `nvim` from the user's `PATH` unless packaging requirements change.
- Do not implement a custom text editor for the MVP.
