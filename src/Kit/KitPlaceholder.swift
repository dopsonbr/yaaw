import Foundation

/// Marker namespace for the YAAWKit library. Real domain types and actor
/// services are added by Chunk 0.3 (domain port) and Chunks A/B/C/E.
public enum YAAWKit {
    /// Human-readable name of the library, used in diagnostics.
    public static let name = "YAAWKit"
}
